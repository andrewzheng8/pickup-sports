<%= form_for(@sport) do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "name", class: "form-control" %>
  </div>
  <%= f.submit "Create Sport", class: "btn btn-default" %>
<% end %>
